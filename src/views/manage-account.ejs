<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8" />
  <meta name="viewport" content="width=device-width,initial-scale=1.0" />
  <title>Manage Account</title>
  <link rel="stylesheet" href="/viewCss/login.css" />
  <script src="/viewJs/login.js" defer></script>
</head>
<body>
  <div class="login-container">
    <h1>Manage Account</h1>
    <% if (success && success.length) { %>
      <span class="flash-message success"><%= success[0] %></span>
    <% } %>
    <% if (error && error.length) { %>
      <span class="flash-message error"><%= error[0] %></span>
    <% } %>
    <form class="login-form" action="/dashboard/manage-account" method="POST" autocomplete="off">
      <div class="form-group">
        <label for="username">Username</label>
        <input id="username" name="username" type="text" value="<%= user.username %>" required />
      </div>
      <div class="form-group">
        <label for="currentPassword">Current Password</label>
        <input id="currentPassword" name="currentPassword" type="password" required />
      </div>
      <div class="form-group">
        <label for="newPassword">New Password</label>
        <input id="newPassword" name="newPassword" type="password" required />
      </div>
      <button class="login-btn" type="submit">Update</button>
    </form>
    <div class="login-footer">
      <a href="/dashboard">← Back to Dashboard</a>
    </div>
  </div>
</body>
</html>